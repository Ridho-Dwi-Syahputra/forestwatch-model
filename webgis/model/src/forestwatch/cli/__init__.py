"""CLI entry points untuk forestwatch.

Dipakai oleh ``[project.scripts]`` di ``pyproject.toml``:
- ``fw-validate`` → ``forestwatch.cli.validate:main``
- ``fw-dummy``    → ``forestwatch.cli.dummy:main``
"""
