fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0
python-multipart>=0.0.6
geopandas>=0.14
shapely>=2.0
python-dotenv>=1.0

# --- Database (dashboard data + log/hasil "Analisis Wilayah Custom") ---
# sqlalchemy: ORM. pymysql: driver MySQL pure-Python (tak perlu compiler C di Windows, beda
# dgn mysqlclient). cryptography: dibutuhkan PyMySQL utk auth plugin caching_sha2_password
# (default MySQL 8+) -- tanpa ini, koneksi gagal walau kredensial benar.
sqlalchemy>=2.0
pymysql>=1.1
cryptography>=41

# --- Fitur "Analisis Wilayah Custom" (POST /api/analyze) ---
# earthengine-api: auth Service Account + tarik komposit Sentinel-2 dari GEE
# rasterio: baca/tulis GeoTIFF (komposit citra + mask hasil inferensi)
# torch + segmentation-models-pytorch: load checkpoint & jalankan inferensi (reuse paket forestwatch)
# requests: unduh GeoTIFF dari ee.Image.getDownloadURL()
earthengine-api>=0.1.400
rasterio>=1.3
torch>=2.0
segmentation-models-pytorch>=0.3
requests>=2.31

# --- Dependensi core package `forestwatch` (di-vendor ke ./model, dipakai bridge) ---
# numpy: array citra/mask. tqdm: progress bar -- DIIMPOR di level atas oleh
# forestwatch.inference.tile_inference & change_detection (tanpa ini, bridge gagal
# import diam-diam -> /api/analyze balas 503). pillow & pyyaml: dipakai submodul
# forestwatch lain (outputs.landcover_png / config.load_config) bila dipanggil.
numpy>=1.24,<3
tqdm>=4.65
pillow>=10.0
pyyaml>=6.0
