# Production dependencies — pin minor untuk reproduktibilitas
# Install: pip install -r requirements.txt
# Atau lebih baik: pip install -e ".[all]"
#
# JETSON ORIN (JetPack 5.x / Python 3.8 / torch 2.1): JANGAN pakai file ini.
# torch/torchvision/onnxruntime disediakan JetPack (wheel aarch64+CUDA dari NVIDIA);
# meng-install ulang via pip akan menimpa build CUDA. Di Jetson cukup:
#     pip install -e ".[jetson]"      # smp, albumentations, torchmetrics, sklearn, matplotlib, onnx
# (torch 2.1 bawaan JetPack sudah memenuhi pin segmentation-models-pytorch.)

# Core (selalu)
numpy>=1.24,<3
pillow>=10.0
pyyaml>=6.0
shapely>=2.0
tqdm>=4.65

# GIS (untuk inference + outputs)
rasterio>=1.3
geopandas>=0.14
fiona>=1.9
pyproj>=3.6

# Deep learning (untuk training + inference)
torch>=2.1
torchvision>=0.16
segmentation-models-pytorch>=0.3.3
albumentations>=1.3
torchmetrics>=1.2
scikit-learn>=1.3
matplotlib>=3.7
onnx>=1.15
onnxruntime>=1.16

# Google Earth Engine (untuk dataset preparation)
earthengine-api>=0.1.380
geemap>=0.30
